<style lang="less">

</style>

<template>
  <view>
    <view class="panel">
      <navigator url="/pages/provider/venues" hover-class="navigator-hover">{{i_venues}}</navigator>
    </view>
    <view class="panel">
      <navigator url="/pages/provider/newactivity" hover-class="navigator-hover">{{i_newactivity}}</navigator>
      <navigator url="/pages/provider/history" class="last-navigator" hover-class="navigator-hover">{{i_history}}</navigator>
    </view>
    <text class="sub-title">{{i_ongoing}}</text>
  </view>
</template>
<script>
  import wepy from 'wepy'
  export default class Index extends wepy.page {
    data = {
      i_newactivity: "provider_newactivity".i,
      i_venues: "provider_venues".i,
      i_history: "provider_history".i,
      i_ongoing: "provider_ongoing".i,
    }
    onLoad() {
      wepy.setNavigationBarTitle({
        title: "provider_title".i
      });
    }
  }
</script>
